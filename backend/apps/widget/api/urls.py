"""
URL patterns for the widget API (mounted at widget/).
"""

from django.urls import path

app_name = 'widget'

urlpatterns = [
    # Will be populated with widget config, chat, and stream endpoints
]
