"""
URL patterns for the conversations API (mounted at api/v1/conversations/).
"""

from django.urls import path

app_name = 'conversations'

urlpatterns = [
    # Will be populated with query, search, and conversation endpoints
]
