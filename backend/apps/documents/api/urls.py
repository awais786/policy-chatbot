"""
URL patterns for the documents API (mounted at api/v1/documents/).
"""

from django.urls import path

app_name = 'documents'

urlpatterns = [
    # Will be populated with document CRUD endpoints
]
